# Trend-MVP (10/50 SMA Crossover)

Upload/paste CSV (`Date,Close`) and get BUY/SELL crossovers with a quick backtest.

## Run locally
Open `web/index.html` in a browser, or use a static server:
```bash
npm i -g serve
serve web
